import Standard.Test.Unit (assertTrue);

module body Standard.Test is
    function Main(root: RootCapability): ExitCode is
        surrenderRoot(root);
        assertTrue(true, "expect true");
        return ExitSuccess();
    end;
end module body.