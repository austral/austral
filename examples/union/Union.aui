module Example.Union is
    union Color: Free is
        case Red;
        case Green;
        case Blue;
    end;

    union Number: Free is
        case Integer is
            value: Int32;
        case Float is
            value: Float64;
    end;

    function Main(root: RootCapability): RootCapability;
end module.
