module body Example.Record is
    record R: Free is
        X: Int32;
    end;

    function Main(root: Root_Capability): Root_Capability is
        let Rec : R := R(X => 10);
        let Y: Int32 := Rec.X;
        return root;
    end;
end module body.
