module body Example.GenericRecord is
    function Main(root: RootCapability): RootCapability is
        let R: Pair[Int32] := Pair(left => 10, right => 10);
        let V: Int32 := R.left;
        return root;
    end;
end module body.
