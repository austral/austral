module body Example.GenericRecord is
    function Main(): ExitCode is
        let R: Pair[Int32] := Pair(left => 10, right => 10);
        let V: Int32 := R.left;
        return ExitSuccess();
    end;
end module body.
