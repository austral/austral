module body Example.NamedArgument is
    function F(x: Boolean, y: Boolean): Unit is
        return nil;
    end;

    function Main(root: Root_Capability): Root_Capability is
        F(y => false, x => true);
        return root;
    end;
end module body.
