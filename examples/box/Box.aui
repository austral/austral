module Example.Box is
    type Box[T: Type]: Linear;

    generic [T: Type]
    function Make(value: T): Box[T];

    --generic [T: Type]
    --function Unbox(box: Box[T]): T;

    --generic [T: Type]
    --function Swap(box: Box[T], new: T): T;

    function Main(): Unit;
end module.
