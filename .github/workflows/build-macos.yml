name: Build on MacOS
on:
  - push

jobs:
  build-and-test-linux:
    strategy:
      fail-fast: true

    runs-on:
      - macos-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: install OCaml 4.14.11
        uses: ocaml/setup-ocaml@v2
        with:
          ocaml-compiler: 4.14.11