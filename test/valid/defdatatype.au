;; -*- mode: lisp -*-

(defdatatype color ()
  (red)
  (green)
  (blue))

(defdatatype int-or-bool ()
  (int-case i32)
  (bool-case boolean))

(defun main () i32
  (let ((c (austral.kernel:construct color red)))
    )
  (let ((c (austral.kernel:construct int-or-bool bool-case true)))
    )
  0)
