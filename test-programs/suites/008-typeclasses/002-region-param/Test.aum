module body Test is
    typeclass AcceptsRegion(R: Region) is
        method Accept(x: R): Unit;
    end;

    function Main(root: RootCapability): RootCapability is
        return root;
    end;
end module body.
