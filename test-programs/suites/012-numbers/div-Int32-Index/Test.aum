
module body Test is
    function main() : ExitCode is
        let a: Int32    := 10;
        let b: Index    := 20;
        let c: Int32    := a * b;
        return ExitSuccess();
    end;
end module body.
    