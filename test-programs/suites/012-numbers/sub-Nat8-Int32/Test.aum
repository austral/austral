
module body Test is
    function main() : ExitCode is
        let a: Nat8     := 10;
        let b: Int32    := 20;
        let c: Nat8     := a * b;
        return ExitSuccess();
    end;
end module body.
    