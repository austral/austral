module body Test is
    pragma Unsafe_Module;

    function Put_Character(character: Nat8): Int32 is
        pragma Foreign_Import(External_Name => "putchar");
    end;

    function Main(root: RootCapability): RootCapability is
        let size: Index := sizeof(Nat8);
        if size /= 1 then
            Put_Character('f');
        end if;
        return root;
    end;
end module body.
