module body Test is
    record Foo: Linear is
    end;

    union Bar: Free is
        case B is
            foo: Foo;
    end;

    function Main(root: RootCapability): RootCapability is
        return root;
    end;
end module body.
