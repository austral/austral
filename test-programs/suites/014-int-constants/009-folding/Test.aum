module body Test is
    function main(): ExitCode is
        let a: Nat8 := 4 + 2;
        let b: Nat8 := 4 - 2;
        let c: Nat8 := 4 * 2;
        let d: Nat8 := 4 / 2;
        let e: Nat8 := ((((4 + 2) - 2) * 2) / 2);
        printLn(a);
        printLn(b);
        printLn(c);
        printLn(d);
        printLn(e);
        return ExitSuccess();
    end;
end module body.
