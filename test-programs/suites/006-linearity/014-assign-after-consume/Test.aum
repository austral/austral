module body Test is
    record Foo: Linear is end;

    function main(): ExitCode is
        let foo: Foo := Foo();
        foo := Foo();   -- Assign
        let {} := foo;  -- Consume
        foo := Foo();   -- Assign
        let {} := foo;  -- Consume
        return ExitSuccess();
    end;
end module body.
