module body Test is
    record R : Linear is
        x: Int32;
    end;

    function Main(root: RootCapability): RootCapability is
        let r: R := R(x => 32);
        if true then
            skip;
        else
            let { x: Int32 } := r;
        end if;
        return root;
    end;
end module body.
