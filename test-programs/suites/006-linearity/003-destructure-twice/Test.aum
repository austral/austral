module body Test is
    record R : Linear is
        x: Int32;
    end;

    function Main(root: RootCapability): RootCapability is
        let r: R := R(x => 32);
        let { x: Int32 } := r;
        let { x: Int32 } := r;
        return root;
    end;
end module body.
