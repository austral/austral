module body Test is
    record R : Linear is
        x: Int32;
    end;

    function Main(root: Root_Capability): Root_Capability is
        let r: R := R(x => 32);
        return root;
    end;
end module body.
