module body Test is
    record R: Free is
        x: Nat8;
        y: Int8;
    end;

    function Main(root: RootCapability): RootCapability is
        let r: R := R(x => 10, y => 10);
        return root;
    end;
end module body.
