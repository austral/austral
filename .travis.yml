language: generic
sudo: false

install:
  - sudo apt-get install -y make smlnj mlton clang++

script:
  - make boreal
  - rm -rf vendor
  - make mlton-test
  - ./boreal examples/test.au --output=test.cpp
